# 失物招领用户端 — 回归测试清单

> 配套：[修复文档-2026-09-14.md](./修复文档-2026-09-14.md)（R-xx 编号为该文档中的问题编号）
> 每完成一项打勾；发现回归问题记录到文末"问题记录"。

## 〇、环境准备

- [ ] 启动后端：`../lost_and_found`（Flask + MySQL + Redis + MinIO，默认 `http://127.0.0.1:5000`）
- [ ] 本项目 `npm install`
- [ ] HBuilderX 运行到 **H5 浏览器**，完成一轮下面的 [P0] 用例
- [ ] HBuilderX 运行到 **微信小程序模拟器**（需在 manifest.json 填 appid），重点复跑标记 [跨端] 的用例
- [ ] （可选）真机调试：`config/index.js` 的 `DEV_LAN_BASE_URL` 填电脑局域网 IP

## 一、认证（R-21 / R-29 / R-30）

- [ ] [P0] 登录失败（错误学号/密码）：提示后端返回的错误文案，**不再出现"登录已过期"误导提示**，也不跳转登录页
- [ ] 登录成功后 switchTab 进入首页，tabBar 正常
- [ ] 注册：验证码 60s 倒计时正常；倒计时中**返回上一页再进入**，无残留定时器报错
- [ ] 注册：填已注册邮箱点发码 → 提示"该邮箱已被注册"，**不进入倒计时**
- [ ] 注册成功后返回登录页（页面栈 login→register，**按返回不再出现第二个 login**）
- [ ] 重置密码：新密码少于 6 位被拦截；重置成功后能登录
- [ ] [跨端] 小程序端登录后**停留 10 分钟内**随意操作多个页面：不出现"登录已过期"闪踢（验证 R-03 parseJwt 在非 H5 端正常解析 token）

## 二、首页（R-17 ~ R-20 / R-31 / R-19）

- [ ] [P0] 首次进入首页：Network 面板只有**一轮**列表+轮播请求（无 onLoad/onShow 双载）
- [ ] 失物 tab 触底加载到第 2 页后**立刻切到招领 tab**：招领列表首条仍为第 1 页数据（跨 tab 竞态修复）
- [ ] 应用高级筛选后，**打开面板改条件但不点"应用"**，触底翻页：第 2 页与第 1 页条件一致
- [ ] 触底加载中快速连点/连续滚动：无重复数据、无跳页；断网后触底，恢复网络再触底能补回该页
- [ ] 搜索关键字 → 点进详情 → 返回：列表仍保持搜索结果（或按产品预期刷新）
- [ ] 公告栏显示 5 条；公告列表页下拉刷新可用
- [ ] 列表滚动翻页时图片/状态标签不错位（:key 修复）

## 三、发布与编辑（R-01 / R-24 ~ R-26）

- [ ] [P0] 发布失物：**换图后图片预览立即更新**，提交后详情页显示新图
- [ ] [P0] 编辑失物/拾物：换图后保存，详情页图片更新
- [ ] 发布按钮**快速双击**：只产生一条发布（Network 只一个 POST）
- [ ] 全部输入框只填空格提交：被拦截
- [ ] 后端校验失败（如超长标题）：toast 显示**后端的具体错误文案**，不是"发布失败，请稍后再试"
- [ ] 上传图片接口断开时：不再把错误文本存为图片地址
- [ ] 直接访问编辑页且不带 id（H5 改 URL 测试）：提示"缺少物品ID"并返回
- [ ] 我的发布：状态开关（确认已找到/已归还）点击后**立即**生效，无 1 秒延迟

## 四、详情（R-22 / R-23）

- [ ] [P0] 自己发布的物品：显示 编辑/删除/状态按钮（验证 user_id 类型比较修复）
- [ ] 别人的物品：只显示"联系发布者"
- [ ] 编辑保存后返回详情：内容为**新数据**（onShow 刷新）
- [ ] 公开物品 user_id 缺失时（如可构造）：提示"暂时无法联系发布者"，不跳转坏链接

## 五、聊天（R-02 / R-07 ~ R-13 / R-27 / R-28）

- [ ] [P0] Socket 断开状态发消息（开发者工具切 Offline 再发）：消息走 HTTP 降级**真正发出**，对方能收到
- [ ] [P0] 发送失败场景：气泡显示"发送失败"（不再是假装已发出）
- [ ] 发送瞬间气泡显示"发送中..."，回显后变"已读/未读"
- [ ] 与 A 聊天期间 C 发来私信：**当前界面不出现 C 的消息气泡**（会话过滤修复）
- [ ] 聊天页切后台（onHide）再回前台：消息正常收发；反复进出多次后**无重复气泡、无重复请求**（监听器泄漏修复）
- [ ] 聊天中断网 10 秒再恢复：自动重连后**继续能收到对方新消息**（_joinedRooms 重建修复）——重点用例
- [ ] 退出聊天页后：对方发消息，消息 tab 角标/列表未读数正常（房间正确退出）
- [ ] 历史消息下拉翻页：无重复消息；断网翻页后恢复，再翻不跳页
- [ ] 时间分隔线在 iOS 真机上正常显示（parseTime 修复，无模拟器可留到真机）
- [ ] 数字/字符串 ID 混合场景：自己的气泡在右侧、头像正确

## 六、我的（R-14 / R-15 / R-31）

- [ ] [P0] 更换头像：上传成功（带 token，不 401），头像刷新
- [ ] [P0] 退出登录后：**对方再发私信不送达本机**；用原账号收不到新消息通知
- [ ] 改邮箱弹窗：倒计时中关闭再打开，按钮仍显示剩余秒数且禁用；倒计时结束前再次发码不会出现秒数每秒减 2
- [ ] 修改姓名/密码正常

## 七、登录态与请求层（R-04 ~ R-06 / R-16 / R-21）

- [ ] 手动把 storage 中 token 改为过期值后请求：静默刷新成功，业务请求正常完成
- [ ] token 和 refreshToken 都改为无效值后请求：提示"登录已过期"并 **reLaunch** 到登录页（页面栈干净，且只跳一次）
- [ ] 冷启动（杀掉小程序/H5 刷新）后直接进"消息"tab：Socket 能连上（App.vue 恢复 userInfo）
- [ ] 任意页面连续触发多个 401：不叠加多个登录页

## 八、构建（本轮新增）

- [ ] `npm run build:h5`（或 `npx vite build --config vite.config.verify.js`，env 见该文件头注释）构建成功
- [ ] HBuilderX 发行到微信小程序：微信开发者工具打开 `unpackage/dist/build/mp-weixin` 正常预览

## 问题记录

| 日期 | 用例 | 现象 | 处理 |
| --- | --- | --- | --- |
|  |  |  |  |
