# 贡献指南

感谢关注「校园失物招领」用户端项目！欢迎通过 Issue 与 Pull Request 参与贡献。

## 如何贡献

1. **提交 Issue**：发现 Bug 或有功能建议，请先搜索现有 Issue，避免重复；新建 Issue 时请说明复现步骤、运行端（H5 / 小程序 / App）与期望行为。
2. **Fork 与分支**：Fork 本仓库后，从 `master` 拉出功能分支进行开发，分支名建议 `feat/xxx` 或 `fix/xxx`。
3. **提交 Pull Request**：PR 请关联对应 Issue，描述清楚改动内容与验证方式。

## 开发约定

- 技术栈为 uni-app（Vue 3）+ Vuex 4 + uni-ui，请保持既有代码风格与目录组织（页面在 `pages/`、通用组件在 `components/`、请求与工具在 `utils/`、状态在 `store/`）。
- 后端地址统一通过 `config/index.js` 配置，请勿在业务代码中硬编码接口地址。
- 提交前请在 H5 与微信小程序两端至少各验证一次核心流程（登录、发布、详情、聊天）。

## 提交信息

使用简洁明确的中文或英文提交说明，推荐格式：

```
<类型>: <简要说明>
```

类型包括：`feat`（新功能）、`fix`（修复）、`docs`（文档）、`style`（样式）、`refactor`（重构）、`chore`（构建/杂项）。

## 行为准则

请友善、尊重每一位参与者，共同维护开放的社区氛围。
