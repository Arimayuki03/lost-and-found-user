# 失物招领用户端 — 第二轮问题修复文档

> 修复日期：2026-09-14
> 修复范围：`lost_and_found_user`（uni-app Vue 3 用户端，manifest.json `vueVersion: "3"`）
> 排查方式：全量阅读 16 个页面（约 1.1 万行）+ 请求层 / API 层 / store / Socket.IO 服务，并对照后端 `lost_and_found` 源码核实各接口实际响应字段。
> 关联文档：上一轮修复见 [修复文档.md](./修复文档.md)（2026-09-13，F-01～F-07）。**注意：上一轮 F-07 声称"消息模块双份实现已对齐端点"，实际未完成**，`utils/api.js` 中 `message` 模块仍残留旧端点（`/user/send`、`/user/read/{id}`、`/user/chat-list`），仅因 main.js 用 `api/message.js` 覆盖 `$api.message` 而成为死代码，本轮未删除（见遗留事项 L-04）。

## 一、问题总览

| 编号 | 严重级别 | 问题 | 状态 |
| --- | --- | --- | --- |
| R-01 | P0 | edit 页与 my-publish 页共 10 处 `this.$set`，Vue 3 下必抛 TypeError：编辑页换图 100% 失败、我的发布改状态失效 | ✅ 已修复 |
| R-02 | P0 | `chat.vue` HTTP 降级发送调用不存在的 `$api.sendMessage`，Socket 断开时消息静默丢失 | ✅ 已修复 |
| R-03 | P1 | `parseJwt` 依赖 `atob`，小程序/App 端无此 API → 每次请求都误判 token 过期并触发刷新 | ✅ 已修复 |
| R-04 | P1 | `request.js` 401 刷新重试无次数上限，可无限循环 | ✅ 已修复 |
| R-05 | P1 | GET 请求参数双拼（手工拼 query 后 uni.request 再拼一次） | ✅ 已修复 |
| R-06 | P1 | 401 后不关闭 Socket 长连接、`navigateTo` 反复叠加登录页 | ✅ 已修复 |
| R-07 | P1 | Socket 断线重连后 `_joinedRooms` 缓存不清空 → 重连后收不到该会话一切消息 | ✅ 已修复 |
| R-08 | P1 | Socket `disconnect` 时置 `socket = null` 但未真正断开，可产生双连接 → 消息重复回调 | ✅ 已修复 |
| R-09 | P1 | `joinChatTargetRoom` 每次注册匿名监听器且永不清理（泄漏累积），且对 room:null 无校验 | ✅ 已修复 |
| R-10 | P1 | `chat.vue` `isCurrentChat` 两个条件重复（复制粘贴错误）+ 非当前会话消息被插入当前聊天界面 | ✅ 已修复 |
| R-11 | P1 | `chat.vue` onHide/onUnload 用用户 ID 调 `leaveRoom`，私聊房间从未真正退出 | ✅ 已修复 |
| R-12 | P1 | `chat.vue` onShow 把房间名当用户 ID 传入 `joinChatTargetRoom`，发出 room:null 畸形请求 | ✅ 已修复 |
| R-13 | P1 | `chat.vue` initWebSocket 轮询定时器/滚动定时器不随生命周期清理 | ✅ 已修复 |
| R-14 | P1 | `my.vue` 头像上传未携带 Authorization header，必然 401 | ✅ 已修复 |
| R-15 | P1 | 退出登录不关闭 Socket.IO 长连接，旧 token 连接继续接收私信 | ✅ 已修复 |
| R-16 | P1 | `userInfo` 仅存内存、`App.vue onLaunch` 为空 → 冷启动后有 token 无 userInfo，聊天 Socket 初始化静默失败 | ✅ 已修复 |
| R-17 | P1 | `index.vue` loadMore 无重入锁、失败不回滚页码、跨 tab 竞态 | ✅ 已修复 |
| R-18 | P1 | `index.vue` 高级筛选翻页读取面板当前值而非"应用"时刻快照 | ✅ 已修复 |
| R-19 | P1 | store 公告/联系人响应字段解析与后端实际返回不符 | ✅ 已修复（已对照后端源码） |
| R-20 | P1 | `index.vue` onLoad+onShow 双重加载，onShow 无条件覆盖搜索/筛选/分页状态 | ✅ 已修复（首显跳过） |
| R-21 | P2 | `utils/api.js` 401 处理与 request.js 不一致：不清 refreshToken、登录接口 401 误报"登录已过期"、reject 形状不一致 | ✅ 已修复 |
| R-22 | P2 | `detail.vue` `isMyItem` 严格相等比较，类型不一致时自己的物品丢失管理入口 | ✅ 已修复 |
| R-23 | P2 | `detail.vue` 编辑保存返回后不刷新；`contactOwner` 不校验 user_id | ✅ 已修复 |
| R-24 | P2 | `publish.vue` 错误提示取 `error.data?.error` 恒 undefined、无防重复提交、校验不 trim、识别失败静默 | ✅ 已修复 |
| R-25 | P2 | `publish/edit` 上传响应兜底把任意含 "http" 的文本当图片 URL 存库 | ✅ 已修复 |
| R-26 | P2 | edit 页 `formData = res.data` 整体回传（混入 id/user_id 等只读字段）、缺 id 可提交到 `/user/lost-items/null` | ✅ 已修复 |
| R-27 | P2 | `chat.vue` 发送中/发送失败状态无 UI 展示，失败消息不可见 | ✅ 已修复 |
| R-28 | P2 | `chat.vue` loadMore 无去重、失败后 offset 不回滚；时间解析在 iOS 上产生 Invalid Date | ✅ 已修复 |
| R-29 | P2 | `register.vue` 验证码倒计时 timer 泄漏、查重失败空 catch 照发验证码、成功后 navigateTo 叠加登录页栈 | ✅ 已修复 |
| R-30 | P2 | `reset-password.vue` 用 Vue 2 的 `beforeDestroy`（Vue 3 不执行）、新密码无长度校验 | ✅ 已修复 |
| R-31 | P2 | `index.vue` 列表 `:key="index"` ×3；`my.vue` 邮箱倒计时可产生双定时器 | ✅ 已修复 |
| R-32 | P2 | `feedback.vue` 获取失败后 `isLoaded` 永不置位（空白页）；`chat-list.vue` 单个头像失败误清空整个列表、unreadRes 无空值保护 | ✅ 已修复 |
| R-33 | P2 | `my-publish.vue` onLoad+onShow 双载、未登录仍发请求；公告列表下拉刷新未在 pages.json 启用（死代码） | ✅ 已修复 |

## 二、修复详情

### R-01（P0）Vue 3 下调用已删除的 `this.$set`，共 10 处

**现象**：Vue 3 已移除 `$set`。编辑页 `pages/edit/editLostItem.vue:202`、`pages/edit/editFoundItem.vue:202` 在图片上传成功后调用 `this.$set(this.formData, 'image_url', imageUrl)` 必抛 `TypeError`，被外层 try/catch 吞掉后提示"图片上传处理失败"，`image_url` 永远不会被赋值——**编辑页换图功能 100% 失败**。复核时另发现 `pages/my-publish/my-publish.vue` 的 `updateItemStatus` 中还有 8 处同类调用，导致"确认已找到/已归还"的本地状态更新同样必抛异常（幸有后续 `loadData()` 兜底刷新，表现为状态延迟 1 秒才更新）。

**修复**：
- 全部 10 处改为直接属性赋值（Vue 3 Proxy 响应式无需 `$set`）。
- 编辑页回填数据改为白名单拷贝（见 R-26），`formData` 的键在 data() 中完整声明，直接赋值完全响应式。

### R-02（P0）HTTP 降级发送调用不存在的方法，Socket 断开时消息静默丢失

**现象**：`pages/chat/chat.vue:578` 在 Socket.IO 不可用时降级调用 `this.$api.sendMessage(...)`。`utils/api.js` 顶层没有 `sendMessage`（它在 `message` 子模块中，而 `$api.message` 被 `api/message.js` 覆盖），调用必抛 TypeError 被吞掉——消息标记 `send_failed` 但请求从未发出；同时模板只渲染 `is_read`，不渲染 `send_failed`/`is_sending`，**用户看到消息"已发出"，实际已丢失，且输入框已清空无法找回**。

**修复**：改为 `this.$api.message.sendMessage(...)`（已对照后端 `/user/chat/message`：入参 `receiver_id`/`message`、返回 `{success, message_id}` 均匹配）。并配合 R-27 在模板上补齐发送状态展示。

### R-03（P1）JWT 解析依赖 `atob`，非 H5 端整体失效

**现象**：`utils/request.js` 的 `parseJwt` 使用浏览器 `atob`。微信小程序与 App(JSCore) 端没有该全局函数 → 解析必失败 → `isTokenExpired()` 在 payload 为 null 时返回 true → **非 H5 端每个请求都误判 token 已过期，先触发一次 token 刷新**（延迟翻倍、刷新接口压力翻倍），刷新一旦因网络抖动失败即被误踢回登录页。

**修复**：新增纯 JS 的 `base64ToBinaryString()`（手工查表解码，兼容省略 `=` padding），`parseJwt` 不再依赖 `atob`。已用含 UTF-8 中文名的 JWT 样本验证解码正确、非法 token 正确返回 null。

### R-04（P1）401 刷新重试无上限

**现象**：`utils/request.js` 收到 401 后无条件刷新并重试，若刷新成功但请求仍 401（如账号被注销），会无限"刷新→重试"循环。

**修复**：`request(options, _retryCount = 0)` 增加内部重试计数，最多重试 1 次。

### R-05（P1）GET 参数双拼

**现象**：`utils/request.js` 手工把 `data` 序列化进 URL 后，`requestOptions` 仍携带原始 `data`，uni.request 对 GET 会再次序列化拼接 → 参数重复发送（`?a=1&a=1`）。

**修复**：GET 请求手工拼完 query 后置 `requestOptions.data = undefined`。

### R-06（P1）401 后的清理与跳转

**现象**：`handleUnauthorized` 不关闭携带旧 token 的 Socket 长连接；用 `navigateTo` 跳登录页，多次 401 会叠加多个登录页。

**修复**：`handleUnauthorized` 中先 `socketIOService.close()`；跳转改 `reLaunch` 并判断当前页已是登录页则不跳。

### R-07 ~ R-09（P1）Socket.IO 服务生命周期（utils/socketio.js）

**现象**：
1. 断线重连后服务端房间已失效，但 `_joinedRooms` 缓存未清空，`joinChatTargetRoom` 看到"已加入"直接返回 `already_joined`，**不再向服务器发 join → 重连后该会话所有消息静默收不到**。
2. `disconnect` 回调里 `this.socket = null`，但 socket.io 自带 `reconnection: true`，旧连接仍在后台重连；期间再次 `initSocket` 会创建第二个连接 → 双连接双回调，消息重复。
3. `joinChatTargetRoom` 内注册的是**匿名** `join_private_chat_result` 监听器，`off` 移除的却是另一个具名引用 → 监听器永不清理，随会话操作无限累积（泄漏）。

**修复**：
- `disconnect` 与 `close()` 中清空 `_joinedRooms`；`disconnect` 不再置空 socket，保留引用让 socket.io 内置重连工作，重连成功触发 `connect → authenticate → joinRoom` 完整重新入房。
- `joinChatTargetRoom` 改为注册与移除同一个具名函数，超时也走同一清理路径；新增 roomName 为 null 的前置校验（配合 R-12）。
- 删除 `registerDefaultEvents` 中重复注册的空操作 `join_private_chat_result` 全局监听。

### R-10 ~ R-13（P1）聊天页消息链路与资源清理（pages/chat/chat.vue）

**现象与修复**：
1. **isCurrentChat 复制粘贴错误**（458-462 行）：前两个条件完全相同，且缺少"接收方是自己"校验。改为双向匹配：`(sid===tid && rid===mid) || (sid===mid && rid===tid)`。
2. **非当前会话消息混入**：`this.messages.push(data)` 不受 isCurrentChat 约束，与第三人的消息会渲染进当前会话。改为非当前会话只更新未读计数并 `return`。
3. **房间退出无效**（160/176 行）：`leaveRoom(targetUserId)` 传用户 ID，实际私聊房间名是 `chat:a-b`，永远退不出。新增 `socketIOService.leaveChatTargetRoom(targetUserId)` 并在 onHide/onUnload 使用（同时同步 `_joinedRooms` 缓存）。
4. **onShow 畸形加房**（211-215 行）：把生成的房间名 `chat:a-b` 当用户 ID 传给 `joinChatTargetRoom`，`parseInt("chat:1-2")=NaN` → room:null 请求。改为直接传 `targetUserId`。
5. **定时器泄漏**：initWebSocket 的 500ms 加房轮询、10s 超时、initialScrollAttempts 的 7 个 setTimeout 全部存到实例并在 `cleanupWebSocket` 中清理；`setCurrentChatTarget(null)` 是无效调用（空参直接 return），新增 `clearCurrentChatTarget()` 替代。
6. **loadMore 去重与回滚**：合并历史消息按字符串化 id 去重；请求失败回滚 `offset`。
7. **iOS 时间解析**：服务端时间形如 `"2026-09-14 10:00:00"`（无 T/Z），iOS JSCore `new Date()` 得到 Invalid Date → 排序 NaN、分隔线显示异常。新增 `parseTime()` 统一规范化（空格转 T），全部排序比较器、`shouldShowTime`、时间格式化均改用它。
8. **发送状态可见**：模板补齐 `发送中... / 发送失败 / 已读 / 未读` 状态；新增 `isSelfMessage()` 统一转字符串比较，气泡方向与头像不再受 number/string 类型影响。

### R-14 ~ R-16（P1）登录态生命周期

1. **my.vue 头像上传未带 token**（必然 401，且报错被误导为"图片不合规"）：补 `Authorization` header + statusCode 校验，文案改为"头像更新失败"。**register.vue 的头像上传存在同样问题，一并修复**。
2. **退出登录不关 Socket**：`my.vue handleLogout` 先 `socketIOService.close()` 再清理存储；同时删除给不存在属性赋值的死代码（`localIsLoggedIn` 等）。
3. **冷启动无 userInfo**：`App.vue onLaunch` 检测"有 token 但 store 中无 userInfo"时静默 `dispatch('getUserInfo')`，聊天页 Socket 初始化（依赖 `userInfo.id`）不再静默失败。

### R-17 ~ R-20（P1）首页列表（pages/index/index.vue）

1. **loadMore 加固**：新增 `isLoadingMore` 重入锁；失败回滚 `this.page--`；请求发起前捕获 `activeTab` 快照，**修复"失物第 2 页数据被追加进招领列表"的跨 tab 竞态**（handleSearch/selectCategory 同样补快照）。
2. **筛选参数快照**：`applyFilter` 时保存 `appliedFilterParams`/`appliedFilterType`，loadMore 复用快照而非筛选面板当前值——用户"应用筛选"后再改面板不点应用，翻页条件不再漂移。清除筛选（clearSearch/clearAdvancedFilter）同步清空快照。
3. **onShow 首显跳过**：onLoad 标记 `_isFirstShow`，onShow 首次直接跳过，消除背靠背双份请求；返回本页时的刷新行为保留。
4. **`:key` 修正**：失物/招领/筛选结果三个列表 `:key="index"` → `:key="item.id"`，避免翻页时节点复用错乱（图片闪烁/状态错位）。

### R-19（P1）响应字段与后端不符（已对照后端源码核实）

| 接口 | 后端实际返回 | 原前端取值 | 修复 |
| --- | --- | --- | --- |
| `/common/announcements` | `{ items, total }` | store 取 `res.announcements`（恒空） | store 改为 `res.items \|\| res.announcements \|\| []` |
| `/user/chat/contacts` | **裸数组** `[...]`（无联系人时 `{message}`） | store 取 `res.contacts`（恒空） | store 改为 `Array.isArray(res) ? res : (res.contacts \|\| [])` |

页面侧（announcement/list.vue 的 `result.items`、chat-list.vue 的 `Array.isArray(contactsRes)`）本就正确，无需改动。

### R-21（P2）utils/api.js 401 处理一致性

- 401 时补清 `refreshToken`（与 request.js 的 `handleUnauthorized` 对齐）。
- `reject(res.data)` 统一错误形状（原先 401 分支 reject 整个 response）。
- 登录/注册/刷新接口的 401 属于业务失败（如密码错误），豁免"登录已过期"提示与跳转，由页面自行展示（login.vue 的错误取值同步修正为 `error.error`）。
- 跳转改 `reLaunch` + 当前页已是登录页则不跳（refreshToken 的 401 分支同步修正）。

### R-22 ~ R-26（P2）detail / publish / edit 页

- **detail.vue**：`isMyItem` 抽为 `checkIsMyItem()` 统一转字符串比较（原来 `===` 在类型不一致时恒 false，自己的物品看不到编辑/删除按钮）；新增 onShow 从编辑页返回后重新拉取详情（首显跳过防双载）；`contactOwner` 校验 `user_id` 存在。
- **publish.vue**：错误提示 `error.data?.error`（恒 undefined）→ `error.error`，后端校验文案不再丢失；两个提交方法增加 `submitting` 防重；全部必填校验补 trim（纯空格不再通过）；图片识别 fail 分支补提示"识别失败，请手动填写类别"；删除永不触发的 `activated()` 死代码。
- **publish/edit 上传兜底**：响应非 JSON 时仅当整体匹配 `/^https?:\/\/\S+$/` 才当 URL，不再把"恰好含 http 的错误文本"存进 `image_url`。
- **edit 页**：onLoad 缺 id 时提示并返回（不再可提交到 `/user/lost-items/null`）；详情回填改白名单拷贝 8 个表单字段（对照后端 `LostItem.to_dict`/`FoundItem.to_dict` 核实），PUT 不再全量回传 id/user_id/created_at 等只读字段；补提交防重与 trim 校验。

### R-29 ~ R-33（P2）认证页与辅助页

- **register.vue**：倒计时 timer 存实例并在 `onUnload` 清理；邮箱查重空 catch 改为中断发码流程并提示；`verificationTip` 复制粘贴错误文案（"验证码正确"）改为"验证码已发送"；补 data 中缺失的 6 个状态属性（原先写入未声明属性，Vue 3 下非响应式）；注册成功/goToLogin 改 navigateBack（避免 login→register→login 页面栈叠加）。
- **reset-password.vue**：`beforeDestroy`（Vue 3 不执行，原为无效清理）→ `onUnload`；新密码补 6 位长度下限（与注册页一致）；跳转与错误取值同上修正。
- **feedback.vue**：获取历史失败时也置 `isLoaded = true`，失败后不再停留在无提示空白页。
- **chat-list.vue**：单个联系人头像获取失败不再误清空整个列表；`unreadRes` 加空值保护。
- **my-publish.vue**：onLoad+onShow 首显跳过去双载；未登录时 onShow 不再发起注定 401 的请求。
- **pages.json**：公告列表页启用 `enablePullDownRefresh`（页面的 `onPullDownRefresh` 此前是死代码）。

## 三、修复验证

- 19 个改动文件的 `<script>` 均通过 Node 语法解析（`new Function` 编译校验），无括号/语法错误。
- `grep` 复核：`this.$set` 全局清零；`$api.sendMessage`（不带 message 前缀）清零；请求层 `atob` 清零；chat 页 `leaveRoom` 调用清零。
- 新 `parseJwt` 用含 UTF-8 payload 的 JWT 样本验证解码正确、非法 token 正确返回 null。
- store 的公告/联系人字段解析已逐一对照后端 `app/common/announcement.py`、`app/user/chat.py` 源码确认。
- 未能进行真机/模拟器端到端验证（本机无 node_modules/HBuilderX 环境），建议在 HBuilderX 中运行到 H5 与微信小程序模拟器各回归一次：登录 → 首页浏览/筛选/翻页 → 发布（含换图）→ 私聊收发（含断网重连）→ 退出登录。

## 四、遗留事项（本轮未改动，建议后续处理）

| 编号 | 级别 | 事项 |
| --- | --- | --- |
| L-01 | 高 | `config/index.js` 的 `BASE_URL` 仍为 `http://127.0.0.1:5000`。上一轮已收敛到单点，但部署真机/生产前必须改为可配置值（建议按 `process.env.NODE_ENV` 区分或改为运行时配置）；同时小程序端需配置合法域名，token 不应走明文 HTTP |
| L-02 | 中 | `chat.vue` 历史消息为 offset 分页，`refreshMessages` 拉最新 20 条后不回补 offset，极端时序下仍可能丢中间消息。根治方案是后端提供 `before_id` 游标分页 |
| L-03 | 中 | Socket 发送消息无 ack/超时机制：回显丢失时临时消息永远停留在"发送中"（现已可见但不可重试）。建议后端为 `send_private_message` 提供回执，前端补超时重发 |
| L-04 | 中 | `utils/api.js` 内部局部 request 与 `utils/request.js` 并存、行为仍不一致（前者无 token 刷新）；其 `message` 模块残留旧端点（`/user/send` 等）为死代码。建议统一到 request.js 并删除局部封装 |
| L-05 | 低 | chat-list 联系人头像 N+1 串行请求；`request.js` GET 数组参数序列化未处理；my.vue onLoad+onShow 重复拉取用户信息 |
| L-06 | 低 | 注册页学号/邮箱查重结果仅写入 data（本轮已补声明），但模板未渲染，用户不可见；login/register/reset-password 自定义导航的 `statusBarHeight(px) + 60 + 'rpx'` 单位混算在部分机型偏小 |
| L-07 | 低 | my-publish 中文错误串匹配后端文案（脆弱）与 1 秒延迟刷新竞态；input-dialog 的 `getOpenerEventChannel` 无空值保护；announcement/detail 缺 id 时页面空白 |
