# 失物招领用户端 — 问题修复文档

> 修复日期：2026-09-13
> 修复范围：`lost_and_found_user`（uni-app Vue 3 用户端）
> 排查方式：全量阅读请求层 / API 层 / store / Socket.IO 服务，并交叉核对全部页面级 `$api.*` 调用与方法名、全部硬编码地址与直连请求。

## 一、问题总览

| 编号 | 严重级别 | 问题 | 状态 |
| --- | --- | --- | --- |
| F-01 | 高 | 后端地址硬编码 10 处、分布 9 个文件，其中 3 处依赖 uni-app Vue3 下无效的环境变量 | ✅ 已修复 |
| F-02 | 高 | `chat.vue` 调用不存在的 `$api.message.markMessageAsRead`，HTTP 兜底已读回执必然失败 | ✅ 已修复 |
| F-03 | 中 | `utils/api.js` 公告列表接口用 `params` 键传参，uni.request 不识别，分页参数全部丢失 | ✅ 已修复 |
| F-04 | 中 | `utils/request.js` GET 查询串拼接：URL 已含 `?` 时会追加第二个 `?`，产生畸形 URL | ✅ 已修复 |
| F-05 | 中 | 缺少 `package.json`，`socket.io-client` 依赖无声明，CLI/npm 方式编译报「模块找不到」 | ✅ 已修复 |
| F-06 | 低 | `utils/api.js` 请求头携带 `Access-Control-Allow-Origin`（响应头放请求侧，无意义且可能触发多余预检） | ✅ 已修复 |
| F-07 | 低 | 消息模块双份实现（`utils/api.js` 内置 + `api/message.js`），端点不一致，靠 main.js 覆盖兜底 | ✅ 已修复（对齐端点） |

## 二、问题详情与修复方案

### F-01（高）后端地址硬编码，环境变量写法无效

**现象**：`http://127.0.0.1:5000` 硬编码共 10 处：

| 文件 | 位置 |
| --- | --- |
| `utils/request.js` | 第 4 行 |
| `utils/api.js` | 第 1 行 |
| `utils/socketio.js` | 第 14 行（`socketUrl`） |
| `pages/publish/publish.vue` | 第 255 行（图片上传、标签识别共用） |
| `pages/my/my.vue` | 第 459 行（头像上传） |
| `pages/register/register.vue` | 第 235、253 行（头像上传、内容合规检测） |
| `pages/detail/detail.vue` | 第 136 行（`process.env.VUE_APP_API_URL \|\| 'http://127.0.0.1:5000'`） |
| `pages/edit/editLostItem.vue` | 第 94 行（同上写法） |
| `pages/edit/editFoundItem.vue` | 第 94 行（同上写法） |

其中 3 处使用 `process.env.VUE_APP_API_URL` 兜底——这是 vue-cli 的约定，uni-app Vue 3（vite 编译器）不会注入该变量，环境变量分支**永远不会生效**，部署换地址时极易遗漏某一处。

**修复**：

1. 新增统一配置文件 `config/index.js`，集中导出 `BASE_URL`（后续可按 `process.env.NODE_ENV` 扩展多环境）。
2. 上述 9 个文件全部改为 `import { BASE_URL } from '@/config'`，删除本地常量与无效环境变量写法。

**修复后换地址只需改 1 个文件。**

### F-02（高）调用不存在的 API 方法，已读回执 HTTP 兜底失效

**现象**：`pages/chat/chat.vue:1049` 在 Socket.IO 已读通知发送失败时，走 HTTP 兜底：

```js
this.$api.message.markMessageAsRead(messageId)
```

但 `api/message.js`（main.js 中覆盖 `$api.message` 的模块）导出的方法名为 **`markAsRead`**，不存在 `markMessageAsRead`。运行时抛出 `TypeError`，又被外层 `try/catch` 静默吞掉——表现为：Socket 未连接时对方永远收不到已读回执，且无任何报错。

**修复**：改为调用 `this.$api.message.markAsRead(messageId)`。

### F-03（中）公告列表接口传参方式错误，分页/筛选参数丢失

**现象**：`utils/api.js` 中：

```js
getList: (params) => {
  return request({ url: '/common/announcements', method: 'GET', params });  // ❌
}
```

`params` 不是 `uni.request` 的合法选项（应为 `data`），页面的 `page`、`page_size` 等查询参数被整体丢弃，公告列表永远只返回默认第一页。

**修复**：`params` 改为 `data`（`utils/request.js` 对 GET 请求会把 `data` 序列化为查询串）。

### F-04（中）GET 查询串拼接可能产生畸形 URL

**现象**：`utils/request.js` 中：

```js
if (options.data && options.method === 'GET') {
  url += objectToQueryString(options.data);   // 返回形如 '?a=1&b=2'
}
```

若 `options.url` 已含 `?`（如 `utils/api.js` 的 `message.getChatHistory` 手工拼了 `'/user/chat-history?user_id=1'`），拼接结果为 `...?user_id=1?offset=0&limit=20`，后端解析异常。

**修复**：

1. `utils/request.js`：已有 `?` 时改用 `&` 续接，无则用 `?`。
2. `utils/api.js` `message.getChatHistory`：不再手工拼 `?user_id=`，统一放入 `data` 传参（防御性修复，该方法当前为 F-07 所述的兼容层）。

### F-05（中）缺少 package.json，npm 依赖无声明

**现象**：`utils/socketio.js` 使用 `import { io } from 'socket.io-client'`，easycom 规则映射到 `@dcloudio/uni-ui`，但项目无 `package.json`。HBuilderX 内置编译可勉强工作，但换用 CLI（`@dcloudio/uni-cli`）或团队协作时会报模块找不到；README 中要求手动 `npm init` 也从未落地。

**修复**：新增 `package.json`，声明 `socket.io-client` 与 `@dcloudio/uni-ui` 依赖。拉取项目后执行 `npm install` 即可。

### F-06（低）客户端请求头携带 CORS 响应头

**现象**：`utils/api.js` 每个请求都带 `'Access-Control-Allow-Origin': '*'`。该头部属于**响应**头，由服务端返回；客户端携带它毫无作用，还会使请求带上非标准自定义头，可能触发额外的 CORS 预检。

**修复**：移除该请求头，CORS 交由后端（Flask-CORS）处理。

### F-07（低）消息模块双份实现，端点不一致

**现象**：

- `utils/api.js` 内置 `message` 模块：`/user/chat-list`、`/user/chat-history?user_id=`、`/user/send`、`/user/read/{id}` 等；
- `api/message.js`：`/user/chat/contacts`、`/user/chat/history/{id}`、`/user/chat/message`、`/user/chat/mark/{id}` 等。

`main.js` 用 `api/message.js` 整体覆盖 `$api.message`，前者成为死代码；两套端点并存易误导后续开发（F-02 即由此诱发）。

**修复**：本次保持模块结构不变（避免大范围重构引入回归），但将 `utils/api.js` 内置 message 模块中与 `api/message.js` 对应的方法**端点对齐**并标注 `@deprecated`，消除 F-04 的手工拼 URL 隐患。**遗留建议**：后续统一删除 `utils/api.js` 内置 message 模块，改由 `store` 与页面直接 import `api/message.js`。

## 三、修复验证

1. 全局扫描 `127.0.0.1`：仅 `config/index.js` 一处（唯一配置点）。
2. 全局扫描 `$api.*` 方法调用与 API 模块导出方法名逐一比对：无不存在的调用。
3. `announcement.getList`、`message.getChatHistory` 传参方式符合 `uni.request` 规范。
4. `package.json` 存在且依赖完整，`npm install` 后 CLI/HBuilderX 均可解析 `socket.io-client`。

> 功能回归建议：重点验证「聊天页 Socket 断连时的已读回执兜底」「公告列表翻页」「发布/编辑/注册/我的页面的图片上传」四条链路。

## 四、遗留建议（本次未改动）

1. **页面直连请求未走统一层**：`detail.vue`、`editLostItem.vue`、`editFoundItem.vue`、`my.vue`、`register.vue` 等页面直接使用 `uni.request`/`uni.uploadFile`，绕过了 `utils/request.js` 的 token 自动刷新与统一 401 处理，access token 过期时这些请求会直接失败。建议后续逐步收敛到统一请求层。
2. **统一上传工具**：图片上传逻辑（uploadFile + 解析 file_url）在 4 个页面各写一份，建议抽取 `utils/upload.js`。
3. **`uni.getStorageSync('userInfo')` 的清理为空操作**：项目从未写入该键，`request.js`/`api.js` 中的移除逻辑可留可删。
