# 失物招领用户端 — 后续工作计划

> 制定日期：2026-09-14
> 前置文档：[修复文档-2026-09-14.md](./修复文档-2026-09-14.md)（第二轮 33 项修复记录）、[回归测试清单.md](./回归测试清单.md)（回归用例）
> 核心原则：**先让已完成的修复被运行时验证，再做任何新改动**——所有后续工作都被回归测试卡住。

## 一、当前状态快照（2026-09-14）

| 事项 | 状态 |
| --- | --- |
| 第二轮审查修复（33 项，含 2 个 P0） | ✅ 完成，19 个文件改动 |
| Git 版本控制 | ✅ 已建仓，4 个提交，工作区干净 |
| BASE_URL 多环境配置 | ✅ 完成（`PROD_BASE_URL` 为占位值，上线前必填） |
| 静态验证（语法解析 + grep 复核 + parseJwt 样本测试） | ✅ 通过 |
| H5 生产构建（uni-app 4.87 + vite 5.4.21，全部 16 页编译） | ✅ 通过 |
| 运行时回归（HBuilderX → H5 / 微信小程序） | ⬜ **未做，是当前唯一卡点** |
| 遗留项 L-02 ~ L-07 | ⬜ 未处理（见修复文档第四节） |

## 二、工作计划总览

| 顺序 | 工作 | 前置条件 | 执行者 |
| --- | --- | --- | --- |
| 1 | 回归测试（清单 P0 用例优先） | 后端启动 + HBuilderX | 用户为主 |
| 2 | L-04 统一请求层重构 | **步骤 1 通过** | 待安排（可由 AI 执行） |
| 3 | 后端配套 L-02 游标分页 + L-03 消息回执 | 步骤 1 通过（可与 2 并行） | 待安排（前后端均可改） |
| 并行 | 后端 / 管理端代码审查 | 无 | 待安排（可由 AI 执行） |

## 三、步骤 1：回归测试（当前最高优先）

33 项修复目前只过了编译验证，运行时行为尚未验证。**后续任何代码改动都应等回归通过后再开始**，否则基线不干净、出了问题无法归因。

### 必测重点（改动最大、风险最高的两处）

1. **Socket 重连链路**（R-07/R-08）：`disconnect` 处理方式已改变——聊天中断网 10 秒恢复后能否继续收到对方新消息，是本轮最关键的用例（清单第五节）。
2. **聊天消息流**（R-10/R-11）：会话过滤（第三人消息不得混入）与房间退出（切后台后消息走未读数）行为变化大。

### 执行路径

1. 启动后端 `../lost_and_found`（Flask + MySQL + Redis + MinIO，默认 `http://127.0.0.1:5000`）；本项目 `npm install`。
2. HBuilderX 运行到 **H5**，按清单从 `[P0]` 用例开始打勾。
3. 运行到 **微信小程序模拟器**，复跑标记 `[跨端]` 的用例（重点：R-03 parseJwt 非浏览器端、R-14 上传带 token、R-16 冷启动恢复 userInfo）。
4. 发现问题记入清单末尾"问题记录"表，逐个修复并提交。

## 四、步骤 2：统一请求层（遗留 L-04，回归通过后做）

### 为什么是下一个代码动作

本轮 33 个问题中有 6 个（R-02、R-14、R-21、R-24 等）同源于一个根因：**项目并存三套鉴权行为不一致的请求方式**——

1. `utils/request.js`（带 token 刷新、401 重试、统一错误提示）；
2. `utils/api.js` 内部局部 `request`（无刷新，401 行为本轮才对齐）；
3. 各页面散落的裸 `uni.request` / `uni.uploadFile`（publish 上传、edit 详情/保存、detail 全部请求、register 头像上传与合规检测等约 8 处）。

统一掉它等于消灭一整类 bug，而不是逐个补。

### 建议做法

- 图片上传封装为 `utils/upload.js`（自动带 token、统一 401 处理），替换全部裸 `uni.uploadFile`；
- `detail.vue` / `register.vue` 的裸 `uni.request` 改走 `$api` 已有方法（`lostItem.getDetail` / `update` / `delete` 等）；
- 删除 `utils/api.js` 内部局部 `request`，其接口定义改调 `utils/request.js`；
- 顺带清理 `utils/api.js` 中 `message` 模块残留的旧端点死代码（`/user/send` 等，见修复文档开头说明）。

### 为什么必须在回归之后

先让当前代码成为**已验证基线**，重构后拿同一份 [回归测试清单](./回归测试清单.md) 再跑对照——出了问题才知道是重构引入的。

## 五、步骤 3：后端配套（可与步骤 2 并行）

聊天可靠性的最后两块拼图都在后端（`../lost_and_found`）：

| 编号 | 内容 | 前端配套 |
| --- | --- | --- |
| L-02 | 历史消息分页从 offset 改为 `before_id` 游标，根治刷新后 offset 漂移导致的丢消息 | `chat.vue` loadMore 改传 `before_id`（最后一条消息 ID） |
| L-03 | `send_private_message` 增加 ack 回执 | 发送超时判定 + 重发入口，配合已有的"发送失败"UI（R-27） |

两侧代码均可修改，建议作为一个独立提交序列（后端先行、前端跟进、联调用例补进回归清单第五节）。

## 六、并行建议：后端 / 管理端审查

本轮前端审出 33 个问题，而后端（`../lost_and_found`）与管理端（`../lost_and_found_admin`）**从未做过同等强度的审查**。本轮核对接口契约时已发现两处两端不一致（公告 `items`、联系人裸数组，R-19），说明契约层值得系统过一遍。

建议范围：后端鉴权与越权（JWT 配置、user/common 路由的权限校验）、输入校验与 SQL 注入面、文件上传（类型/大小/回显）、Socket 认证、分页与错误响应格式统一；管理端重点看权限边界与上传。产出物沿用本轮格式：审查报告 → 修复文档 → 回归清单。

## 七、上线前检查清单（部署时执行）

- [ ] `config/index.js` 的 `PROD_BASE_URL` 改为真实服务器地址，并使用 **HTTPS**（当前 JWT 走明文 HTTP）
- [ ] 微信小程序后台配置 request/uploadFile/downloadFile 合法域名 + Socket 合法域名（wss）
- [ ] `manifest.json` 的 `mp-weixin.appid` 填入正式 appid
- [ ] 真机回归一轮清单中 `[跨端]` 用例（尤其 iOS 真机的聊天时间分隔线）
- [ ] 后端对应检查：生产密钥（JWT SECRET 等）、CORS 收紧、MinIO/数据库备份策略
